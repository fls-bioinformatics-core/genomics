NGS-general
===========

General NGS scripts that are used for both ChIP-seq and RNA-seq.

cd_set_umask_ngsdata.sh: script to set and revert umask appropriately
when moving in and out of the NGS data directory.

explain_sam_flag.sh: convert a decimal bitwise SAM flag value to binary
representation and interpret each bit.
