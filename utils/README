utils
=====

Place to put utility scripts/tools.

cd_set_umask_ngsdata.sh
-----------------------
Script to set an alias which sets and reverts user's umask appropriately
when moving in and out of the NGS data directory.